.empty { padding: var(--space-12) var(--space-4); text-align: center; }
.empty-icon { font-size: 48px; margin-bottom: var(--space-3); opacity: 0.4; }
.empty-text { color: var(--color-text-tertiary); font-size: var(--font-sm); }