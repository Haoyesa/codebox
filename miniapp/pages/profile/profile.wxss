.title { font-size: var(--font-lg); font-weight: var(--font-semibold); margin-bottom: var(--space-2); }
.value { font-size: var(--font-base); color: var(--color-text); margin-bottom: var(--space-3); }
.link { color: var(--color-primary); font-size: var(--font-sm); }
.desc { color: var(--color-text-secondary); font-size: var(--font-sm); }