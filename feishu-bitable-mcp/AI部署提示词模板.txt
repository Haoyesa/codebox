【飞书多维表格 MCP 一键部署提示词模板】

请你充当我的本机 MCP 部署助手，严格按步骤执行并给我可直接复制的配置片段。

我的解压路径是：
{{在这里替换为你的真实路径，例如 D:\\tools\\feishu-bitable-mcp\\student-pack-win-x64}}

我的目标客户端是：
{{Codex / Cursor / VSCode / Claude，填一个}}

请按下面顺序输出：
1) 环境检查步骤（Python 3.10+ 是否可用）
2) 安装命令（基于 install.bat）
3) .env 填写说明（APP_ID+APP_SECRET 或 USER_ACCESS_TOKEN 二选一）
4) 对应客户端的 MCP 配置（把路径替换为我的真实路径）
5) 验证步骤（至少给 3 条可直接发送的 MCP 测试指令）
6) 故障排查（路径错误、凭证错误、权限不足）

约束：
- 不要输出空泛解释，直接给可执行步骤。
- 所有路径都用 Windows 反斜杠。
- 明确标出“我需要手动替换的字段”。
